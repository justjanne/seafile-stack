apiVersion: v1
kind: Secret
metadata:
  name: {{ include "seafile.fullname" . }}-fileserver
  labels:
    {{- include "seafile.labels" . | nindent 4 }}
stringData:
  SEAFILE_DB_PROTOCOL: {{ include "seafile.databaseType.jdbc" .Values.fileserver.database.type | quote }}
  SEAFILE_DB_TYPE: {{ .Values.fileserver.database.type | quote }}
  SEAFILE_DB_HOST: {{ .Values.fileserver.database.host | quote }}
  SEAFILE_DB_PORT: {{ .Values.fileserver.database.port | quote }}
  SEAFILE_DB_USER: {{ .Values.fileserver.database.username | quote }}
  SEAFILE_DB_PASSWORD: {{ .Values.fileserver.database.password | quote }}
  REDIS_PASSWORD: {{ .Values.cache.password | quote }}
  JWT_PRIVATE_KEY: {{ .Values.jwtSecret | quote }}
