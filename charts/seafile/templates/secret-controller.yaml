apiVersion: v1
kind: Secret
metadata:
  name: {{ include "seafile.fullname" . }}-controller
  labels:
    {{- include "seafile.labels" . | nindent 4 }}
stringData:
  SEAFILE_DB_PROTOCOL: {{ include "seafile.databaseType.jdbc" .Values.controller.database.type | quote }}
  SEAFILE_DB_TYPE: {{ .Values.controller.database.type | quote }}
  SEAFILE_DB_HOST: {{ .Values.controller.database.host | quote }}
  SEAFILE_DB_PORT: {{ .Values.controller.database.port | quote }}
  SEAFILE_DB_USER: {{ .Values.controller.database.username | quote }}
  SEAFILE_DB_PASSWORD: {{ .Values.controller.database.password | quote }}
  REDIS_PASSWORD: {{ .Values.cache.password | quote }}
  JWT_PRIVATE_KEY: {{ .Values.jwtSecret | quote }}
